name: Run branch test Action kind canonicalization
on:
  push:
    branches: [refactor/remove-action-intent-compat]
permissions:
  contents: write
jobs:
  cleanup:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/remove-action-intent-compat
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Canonicalize test Action terminology
        run: |
          node scripts/_branch_canonicalize_test_action_kind.cjs
          node tests/v11-state-regression.mjs
          node tests/sleep-pressure.mjs
          node tests/social-sleep-interaction.mjs
          node tests/action-terminology.mjs
          node tests/action-construction.mjs
          node tests/runtime-hook-pipeline.mjs
      - name: Commit cleanup
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Canonicalize Action kind in regression fixtures"
          git push origin HEAD:refactor/remove-action-intent-compat
