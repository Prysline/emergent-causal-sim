name: Patch canonical decision utility

on:
  push:
    branches:
      - "refactor/canonical-decision-utility"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/canonical-decision-utility
      - name: Smoke check
        run: echo ready
